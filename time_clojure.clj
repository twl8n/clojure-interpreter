#!/usr/bin/env clojure
;; You must install clojure for this example.
;; 5MB to update brew then, readline 1.5MB, clojure 19.4MB, then 3.4MB additional jar files
;; 1.40 secs via /usr/bin/time
(printf "done.\n")
